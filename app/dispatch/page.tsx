import { requirePageRole } from "../../lib/page-auth";
export const dynamic="force-dynamic";
export default async function DispatchPage(){await requirePageRole(["PLATFORM_ADMIN","PLATFORM_OPERATIONS","DISPATCHER","SUPPORT"],"/dispatch");return <div className="shell"><aside className="nav"><div className="brand">me2u<span>/</span>delivery</div><a className="active" href="/dispatch">Dispatch</a><a href="/courier">Courier app</a><a href="/admin">Platform admin</a></aside><main className="main"><div className="eyebrow">Dispatch command center</div><h1>Assignments</h1><p className="sub">Tenant-scoped delivery operations with server-authorized actions and immutable event history.</p><section className="section card"><h2>Live queue</h2><p className="sub">No active deliveries are currently available to this dispatcher workspace.</p></section></main></div>}
