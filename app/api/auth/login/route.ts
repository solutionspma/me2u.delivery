import { z } from "zod";
import { responseError, responseOk } from "../../../../lib/api";
export async function POST(request:Request) { try { const input=z.object({email:z.string().email(),password:z.string().min(1)}).parse(await request.json()); const base=process.env.YAHBASE_API_BASE ?? "https://yahbase.com"; const upstream=await fetch(`${base}/auth/login`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input),cache:"no-store"}); if(!upstream.ok) return responseError(new Error("UNAUTHENTICATED")); const body=await upstream.json() as {token:string}; const response=responseOk({authenticated:true},200); response.headers.append("set-cookie",`me2u_session=${body.token}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=604800`); return response; } catch(error) { return responseError(error); } }
