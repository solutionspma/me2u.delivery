"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
export default function TrackPage(){const [token,setToken]=useState(""); const [error,setError]=useState(""); function submit(event:FormEvent){event.preventDefault();if(token.trim()) window.location.assign(`/track/${encodeURIComponent(token.trim())}`);else setError("Enter the tracking code from your delivery message.");} return <main className="simple-public"><div className="simple-top"><Link className="public-logo" href="/">me2u<span>/</span></Link><Link href="/login">Sign in</Link></div><section className="center-panel"><div className="eyebrow">DELIVERY VISIBILITY</div><h1>Where's your order?</h1><p>Enter your tracking code to see the latest delivery status.</p><form className="track-form" onSubmit={submit}><label htmlFor="tracking">Tracking code</label><div className="search-row"><input id="tracking" value={token} onChange={event=>setToken(event.target.value)} placeholder="Paste your Me2U tracking code"/><button type="submit">Track delivery <span>→</span></button></div></form>{error&&<div className="form-error">{error}</div>}<div className="empty-note">Your courier's personal information is never shown. We only share what you need to follow your delivery.</div></section></main>}
