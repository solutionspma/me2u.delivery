"use client";
import Link from "next/link";
import { FormEvent,useState } from "react";
export default function CourierSignup(){const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const response=await fetch("/api/onboarding/courier",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});const body=await response.json();setMessage(response.ok?"Application saved for review. We'll guide you through the next steps after sign in.":body.error?.message??"We couldn't save the application.");}return <main className="auth-page"><Link className="public-logo" href="/">me2u<span>/</span></Link><div className="auth-card"><div className="eyebrow">COURIER ONBOARDING</div><h1>Move with Me2U.</h1><p>Apply to deliver locally. Approval is required before going online.</p><form onSubmit={submit}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" type="tel" required/></label><label>Delivery market<input name="market" required placeholder="City or service area"/></label><label>Password<input name="password" type="password" minLength={8} required/></label><button className="primary-button">Start application</button></form>{message&&<div className="auth-help">{message}</div>}</div></main>}
