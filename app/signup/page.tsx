"use client";
import Link from "next/link";
import { FormEvent,useState } from "react";
export default function SignupPage(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(!response.ok){setError("We couldn't create that account. Check the details or use another email.");setBusy(false);return;}window.location.assign("/app");}return <main className="auth-page"><Link className="public-logo" href="/">me2u<span>/</span></Link><div className="auth-card"><div className="eyebrow">CREATE YOUR ACCOUNT</div><h1>Start with local.</h1><p>Shop nearby, save addresses, and follow every delivery.</p><form onSubmit={submit}><label>First name<input name="firstName" required/></label><label>Last name<input name="lastName" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone <span className="muted">optional</span><input name="phone" type="tel"/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-button" disabled={busy}>{busy?"Creating…":"Create account"}</button></form><div className="auth-help">Already have an account? <Link href="/login">Sign in</Link></div></div></main>}
