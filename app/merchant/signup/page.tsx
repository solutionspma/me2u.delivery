"use client";
import Link from "next/link";
import { FormEvent,useState } from "react";
export default function MerchantSignup(){const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const response=await fetch("/api/onboarding/merchant",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});const body=await response.json();if(response.ok){setMessage("APPLICATION RECEIVED — your merchant application is saved as a draft. Sign in to continue setup; Me2U will review it before activation.");return;}const code=body.error?.code;setMessage(code==="EXISTING_ACCOUNT_LOGIN_REQUIRED"?"This email already has a Me2U account. Sign in to continue your merchant application.":code==="IDENTITY_PROVIDER_ERROR"?"We couldn't create the account with Me2U authentication. Please try again or contact support.":body.error?.message??"We couldn't save the application.");}return <main className="auth-page"><Link className="public-logo" href="/">me2u<span>/</span></Link><div className="auth-card"><div className="eyebrow">MERCHANT ONBOARDING</div><h1>Bring your business to Me2U.</h1><p>Create an account and save your application. Activation requires review.</p><form onSubmit={submit}><label>Business name<input name="businessName" required placeholder="Your legal or trading name"/></label><label>Your name<input name="contactName" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" type="tel" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><button className="primary-button">Save application</button></form>{message&&<div className="auth-help">{message}</div>}</div></main>}
