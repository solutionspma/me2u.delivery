import { NextResponse } from "next/server";
export class ApiError extends Error { constructor(public code:string, public status:number, message=code) { super(message); } }
export function responseError(error: unknown, correlationId?: string) { const code=error instanceof ApiError ? error.code : error instanceof Error ? error.message : "INTERNAL_ERROR"; const status=code === "UNAUTHENTICATED" ? 401 : ["FORBIDDEN","TENANT_ACCESS_DENIED"].includes(code) ? 403 : ["NOT_FOUND"].includes(code) ? 404 : ["CONFLICT"].includes(code) ? 409 : ["INVALID_REQUEST","INVALID_TRANSITION","VERIFICATION_REQUIRED","POD_REQUIRED"].includes(code) ? 422 : 500; return NextResponse.json({ error:{ code:status===500?"INTERNAL_ERROR":code, message:status===500?"Internal server error":code }, correlationId }, { status, headers:{"x-request-id":correlationId ?? crypto.randomUUID()} }); }
export function responseOk<T>(body:T, status=200, correlationId?:string) { return NextResponse.json(body, {status, headers:{"x-request-id":correlationId ?? crypto.randomUUID()}}); }
